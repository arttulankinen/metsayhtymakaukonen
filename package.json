{
  "name": "metsayhtymakaukonen",
  "version": "1.0.0",
  "main": "Sender.js",
  "scripts": {
    "start": "cd Backend && node Sender.js",
    "build": "cd Frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "nodemailer": "^6.9.0"
  }
}

